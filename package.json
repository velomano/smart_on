{
  "devDependencies": {
    "@expo/cli": "^54.0.8",
    "@expo/config-plugins": "^54.0.1",
    "@react-native-community/cli": "^20.0.2",
    "@react-native-community/cli-platform-android": "^20.0.2",
    "@react-native/metro-config": "^0.81.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "expo": "~54.0.10",
    "react": "19.1.0",
    "react-native": "0.81.4"
  },
  "name": "smart_on",
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "android": "expo run:android",
    "ios": "expo run:ios",
    "build:web-admin": "npm run build -w apps/web-admin",
    "dev:web-admin": "npm run dev -w apps/web-admin",
    "build:worker": "npm run build -w apps/worker",
    "build:bridge": "npm run build -w apps/mqtt-bridge",
    "verify:profile": "node verify-profile.js"
  }
}
