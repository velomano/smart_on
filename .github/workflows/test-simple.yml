name: 간단한 테스트 워크플로우

on:
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    
    steps:
    - name: 체크아웃
      uses: actions/checkout@v4
      
    - name: 간단한 테스트
      run: |
        echo "🚀 GitHub Actions 테스트 시작"
        echo "현재 시간: $(date)"
        echo "Node.js 버전: $(node --version)"
        echo "✅ 테스트 완료"
